<div class="container">
  <div class="row">
    <h3>ログイン</h3>
  </div>
  <div class="row">
    <p>会員の方はこちらからログイン</p>
  </div>
  <%= form_with model: @customer, url: customer_session_path do |f| %>
    <div class="row">
      <div class="col"><%= f.label "メールアドレス" %></div>
      <div class="col"><%= f.email_field :email, autofocus: true, autocomplete: "email" %></div>
    </div>
    <div class="row">
      <div class="col"><%= f.label "パスワード" %></div>
      <div class="col"><%= f.password_field :password, autocomplete: "current-password" %></div>
    </div>
    <div class="row actions">
      <div class="col"></div>
      <div class="col"><%= f.submit "ログイン" %></div>
    </div>
  <% end %>
  <div class="row">
    <p>会員登録がお済みでない方</p>
  </div>
  <div class="row">
    <p>こちらから新規登録を行ってください。</p>
  </div>
  
  <%= render "public/shared/links" %>
</div>

